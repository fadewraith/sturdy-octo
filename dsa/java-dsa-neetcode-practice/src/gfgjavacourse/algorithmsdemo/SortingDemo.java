package gfgjavacourse.algorithmsdemo;

public class SortingDemo {

    /**
     * Arrays.sort() -> arr of primitive, arr of objects (which type of sorting they implement do r&d)
     * Collections.sort() -> for lists(ArrayList, LinkedList, ...) (which type of sorting they implement do r&d)
     * */

    /***
     * Arrays.sort()
     */
}
