package gfgjavacourse.streamsdemo;

public class StreamsDemo {

    /**
     * BaseStream -> IntStream, LongStream, DoubleStream, Stream<T>
     * BaseStream -> parallel(), sequential(), unordered()
     * Methods in the 4 subinterfaces -> filter(), forEach(), min(), max(), skip(), findFirst(), count(), distinct(), sorted(), reduce(), collect(), groupingBy() etc
     * Methods only in the first 3 subinterfaces -> sum(), average(), boxed()
     * */
}
