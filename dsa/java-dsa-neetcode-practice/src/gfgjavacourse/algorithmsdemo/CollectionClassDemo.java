package gfgjavacourse.algorithmsdemo;

public class CollectionClassDemo {

    /**
     * Collection methods -
     * min(c), max(c), disjoint(c1, c2), frequency(c, obj)
     * ===================================
     * List methods -
     * binarySearch(list, key)
     * copy(dest, src)
     * fill(list, obj)
     * indexOfSubList(sourceL, targetL)
     * lastIndexOfSubList(srcL, targetL)
     * replaceAll(list, old, new)
     * reverse()
     * rotate(list, dist)
     * sort(list)
     * swap(list, i, j)
     * ===================================
     * Other methods -
     * reverseOrder()
     * synchronizedList(l)
     * synchronizedMap(m)
     * synchronizedSet(s)
     * unmodifiableList(l)
     * unmodifiableMap(m)
     * unmodifiableSet(s)
     * ===================================
     * Collections.fill();
     * ===================================
     * Collections.reverse()
     * ===================================
     * Collections.binarySearch(list, key);
     * ===================================
     * */
}
