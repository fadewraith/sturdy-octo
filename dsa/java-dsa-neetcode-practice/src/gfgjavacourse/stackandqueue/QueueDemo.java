package gfgjavacourse.stackandqueue;

public class QueueDemo {

    /**
     * Queue -> An interface implemented by the foll classes -
     * LinkedList, ArrayDeque
     * Queue<> q = new ArrayDeque<>();
     * Functions that do not throw exceptions - peek(), offer(), poll()
     * Functions that throw exceptions - element(), add(), remove()
     * */
}
