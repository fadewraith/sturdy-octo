package gfgjavacourse.algorithmsdemo;

public class ArraysClassDemo {

    /**
     * asList(), binarySearch(), equals(), mismatch(), compare(), fill(), sort(), stream(), toString()
     * */

    /**
     * Arrays.fill(arr, val)
     * Arrays.fill(arr, from, to, val)
     * Collections.fill(arr, val)
     * ================================
     * Arrays.binarySearch(arr, val)
     * Arrays.binarySearch(arr, from, to, val)
     * Arrays.binarySearch(arr, val, new YourCustomComparatorClass())
     * ================================
     * boolean equals(a[], b[])
     * boolean equals(a[], aStart, aEnd, b[], bStart, bEnd)
     * boolean equals(a[], b[], Comparator)
     * boolean equals(a[], aStart, aEnd, b[], bStart, bEnd, Comparator)
     * ================================
     * int mismatch(a[], b[])
     * int mismatch(a[], aStart, aEnd, b[], bStart, bEnd)
     * int mismatch(a[], b[], Comparator)
     * int mismatch(a[], aStart, aEnd, b[], bStart, bEnd, Comparator)
     * ================================
     * int compare(a[], b[])
     * int compare(a[], aStart, aEnd, b[], bStart, bEnd)
     * int compare(a[], b[], Comparator)
     * int compare(a[], aStart, aEnd, b[], bStart, bEnd, Comparator)
     * ================================
     * Arrays.toString()
     * ================================
     * */
}
